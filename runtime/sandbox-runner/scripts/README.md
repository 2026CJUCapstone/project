# scripts

정책 검증 PoC에서 사용하는 빌드/실행 스크립트를 둔다.

## 현재 스크립트

- `build_samples.sh`
  - C 샘플을 `build/samples/` 아래에 컴파일한다.
- `run_sample.sh`
  - 샘플 하나를 제한값과 함께 실행하고 결과를 `reports/`에 기록한다.

## 설계 원칙

- 현재는 최소 PoC 드라이버만 제공한다.
- 실제 샌드박스 엔진이 정해지면 실행 래퍼만 교체할 수 있게 유지한다.
- 외부 API와는 연결하지 않는다.
